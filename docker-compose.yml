version: '3'
services:
  chrome:
    build: ./chrome
    ports:
      - '9222:9222'
  nightwatch:
    build: ./nightwatch
    links:
      - chrome
    volumes:
      - './nightwatch/tests:/tests'
    command: run test-nodemon
